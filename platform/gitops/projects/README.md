# Argo CD projects

The applications currently use Argo CD's `default` project to preserve the
existing deployment behavior. Add an `AppProject` here when repository,
destination and namespace allow-lists are agreed by the platform owners.
