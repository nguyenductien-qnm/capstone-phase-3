reloader:
  enabled: true # Bật Stakater Reloader cho Develop để test auto-reload khi credential xoay vòng

externalSecrets:
  enabled: true
  region: us-east-1
  secretPrefix: ecommerce-develop-dev
  refreshInterval: 15s
  useProxyEndpoint: true # Bật Proxy endpoint cho Develop để test rotation & zero-downtime
  serviceAccountName: external-secrets
  serviceAccountNamespace: external-secrets


