# CI-managed image tags — CHỈ CI (app-build.yaml) ghi file này, KHÔNG sửa tay.
# Tách khỏi values.yaml gốc để bump tag không reformat/phá comment của chart.
# Load SAU values.yaml (helm -f cuối cùng thắng) -> override imageOverride.tag.
# Tag convention: <version>-<service>-<sha>. ArgoCD selfHeal sync khi file này đổi.
components:
  cart:
    imageOverride:
      # Sua tay 1 lan (incident 2026-07-16): tag tran "1.1-cart" bi ECR lifecycle
      # policy xoa -> ImagePullBackOff. cart dang HOAN BUILD (build-exclusions) nen
      # CI khong tu bump; image rebuild tu source, push tag sha. CI se ghi de khi bo hoan.
      tag: "1.1-cart-ae89fa2"
  checkout:
    imageOverride:
      tag: "1.0-checkout-07809af"
  frontend-proxy:
    imageOverride:
      tag: "1.0-frontend-proxy-07809af"
  product-catalog:
    imageOverride:
      tag: "1.0-product-catalog-07809af"
  ad:
    imageOverride:
      tag: 1.0-ad-07809af
  currency:
    imageOverride:
      tag: 1.0-currency-07809af
  accounting:
    imageOverride:
      tag: 1.0-accounting-07809af
  product-reviews:
    imageOverride:
      tag: 1.0-product-reviews-07809af
  llm:
    imageOverride:
      tag: 1.0-llm-07809af
  load-generator:
    imageOverride:
      tag: 1.0-load-generator-07809af
  quote:
    imageOverride:
      tag: 1.0-quote-07809af
  recommendation:
    imageOverride:
      tag: 1.0-recommendation-07809af
  shipping:
    imageOverride:
      tag: 1.0-shipping-07809af
  shopping-copilot:
    imageOverride:
      tag: 1.0-shopping-copilot-07809af
  ml-guard:
    imageOverride:
      tag: 1.0-ml-guard-07809af
