# Shopping Copilot — AIO Team dependencies
# Cài đặt: pip install -r requirements-copilot.txt

streamlit>=1.35.0
boto3>=1.34.0
botocore>=1.34.0

# gRPC integration (TF1-56) — kết nối vào microservice thật trên EKS
grpcio>=1.60.0
grpcio-tools>=1.60.0
