# Mandate-19 Phase 2 catalog step-load — Job v2. CSV to emptyDir, pod stays alive
# after the shape ends so the CSV is readable (v1 lost /tmp on pod exit).
apiVersion: v1
kind: ConfigMap
metadata:
  name: locust-phase2
  namespace: techx-develop
data:
  locustfile.py: |
    #!/usr/bin/python
    # Mandate-19 CATALOG flow + built-in step-load shape. Headless, self-driving.
    # Plan §7.1 (one flow=one class) + §5.4 step load. Emits CSV via --csv so we
    # read per-step RPS/p95 without poking any web API.
    import random
    from locust import HttpUser, task, between, LoadTestShape

    PRODUCTS = ["0PUK6V6EV0","1YMWWN1N4O","2ZYFJ3GM2N","66VCHSJNUP","6E92ZMYYFZ",
                "9SIQT8TOJO","L9ECAV7KIM","LS4PSXUNUM","OLJCESPC7Z","HQTGWGPNH4"]

    class CatalogFlowUser(HttpUser):
        wait_time = between(1, 3)
        @task(3)
        def view_product(self):
            self.client.get(f"/api/products/{random.choice(PRODUCTS)}", name="/api/products/:id")
        @task(1)
        def list_products(self):
            self.client.get("/api/products", name="/api/products")

    class StepShape(LoadTestShape):
        # (end_time_seconds, users, spawn_rate). Plan bậc 5/10/20/40, mỗi bậc 240s.
        # Idle handled by starting at t=0 with 5 users (idle baseline đo riêng trước khi chạy).
        stages = [
            (240,  5, 2),
            (480, 10, 2),
            (720, 20, 3),
            (960, 40, 4),
        ]
        def tick(self):
            t = self.get_run_time()
            for end, users, rate in self.stages:
                if t < end:
                    return (users, rate)
            return None
---
apiVersion: batch/v1
kind: Job
metadata:
  name: locust-phase2
  namespace: techx-develop
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 7200
  template:
    metadata:
      labels: { app: locust-phase2 }
    spec:
      restartPolicy: Never
      nodeSelector: { workload-tier: observability }
      tolerations:
        - { key: dedicated, operator: Equal, value: observability, effect: NoSchedule }
      securityContext: { runAsNonRoot: true, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: locust
          image: 804372444787.dkr.ecr.us-east-1.amazonaws.com/ecommerce-dev-techx-corp:1.0-load-generator-5369fc8
          # override ENTRYPOINT so we can run locust then keep the pod alive for CSV read
          command: ["sh","-c"]
          args:
            - >
              locust -f /mnt/locust/locustfile.py --host http://frontend-proxy:80
              --headless --csv /data/catalog --csv-full-history --run-time 1000s;
              echo "LOCUST_DONE exit=$?";
              sleep 3600
          env:
            - { name: OTEL_COLLECTOR_NAME, value: otel-collector }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://otel-collector:4317" }
            - { name: LOCUST_BROWSER_TRAFFIC_ENABLED, value: "false" }
            - { name: PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION, value: python }
          securityContext: { allowPrivilegeEscalation: false, runAsNonRoot: true, capabilities: { drop: ["ALL"] } }
          resources:
            requests: { cpu: "500m", memory: "512Mi" }
            limits: { cpu: "1", memory: "1Gi" }
          volumeMounts:
            - { name: lf, mountPath: /mnt/locust }
            - { name: data, mountPath: /data }
      volumes:
        - name: lf
          configMap: { name: locust-phase2 }
        - name: data
          emptyDir: {}
