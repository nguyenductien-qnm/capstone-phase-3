# NEGATIVE TEST — vi phạm luật #5: require-cpu-memory-limits-requests (K8sRequiredResources)
# Kỳ vọng: BỊ TỪ CHỐI khi apply vào techx-tf1.
# Chỉ vi phạm ĐÚNG 1 luật (không có block resources); mọi field khác hợp lệ.
apiVersion: v1
kind: Pod
metadata:
  name: neg-03-missing-resources
  namespace: techx-tf1
  labels:
    app.kubernetes.io/part-of: gatekeeper-negative-test
spec:
  restartPolicy: Never
  containers:
    - name: neg-noresources
      image: busybox:1.38.0
      command: ["sleep", "300"]
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        allowPrivilegeEscalation: false
        capabilities:
          drop: ["ALL"]
      # <-- VI PHẠM: không khai báo resources (thiếu cả requests lẫn limits)
