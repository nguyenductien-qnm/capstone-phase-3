apiVersion: v1
kind: Pod
metadata:
  name: psql-drill-loss
  namespace: techx-develop
spec:
  restartPolicy: Never
  securityContext:
    runAsNonRoot: true
    runAsUser: 70
    runAsGroup: 70
    fsGroup: 70
  containers:
    - name: runner
      image: postgres:15-alpine
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
      env:
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: password
      command:
        - sh
        - -c
        - |
          psql -h ecommerce-develop-dev-postgres.cgduc4gcisdx.us-east-1.rds.amazonaws.com -U db_admin -d ecommerce_db -c "DROP SCHEMA IF EXISTS drill_m20 CASCADE;"
          echo "DATA_LOSS_EVENT_COMPLETED"
